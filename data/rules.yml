version: "2.0"
rules:
- rule: out-of-scope
  steps:
  - intent: out_of_scope
  - action: utter_out_of_scope
#- rule: Rule to map `login` intent to `action_validate_auth` (automatic conversion)
#  steps:
#  - intent: login
#  - action: action_validate_auth
- rule: Rule to handle messages with low NLU confidence (automated conversion from 'TwoStageFallbackPolicy')
  steps:
  # action_default_fallback and out_of_scope as default actions for default nlu action and deny intent
  - intent: nlu_fallback
  # The Fallback is now implemented as a form.
  - action: action_two_stage_fallback
  - active_loop: action_two_stage_fallback
  # My custom action
#  - action: action_default_ask_affirmation
#  - active_loop: action_default_ask_affirmation
- rule: Respond to FAQs
  steps:
  - intent: faq
  - action: utter_faq
- rule: Respond to chitchat
  steps:
  - intent: chitchat
  - action: utter_chitchat
